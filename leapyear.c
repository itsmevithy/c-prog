#include<stdio.h>

int main(){
	int year;
	printf("Enter year:");
	scanf("%4d",&year);
	if (year%4==0){
		if(year%100==0){
			if()year
		}
		printf("%d is a leap year\n", year);	
	} 
	else{
		printf("%d is not a leap year\n", year);
	}
}
